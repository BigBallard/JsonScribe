namespace JsonLite.IO
{
    public enum TokenType
    {
        Colon,
        Comma,
        CurlyOpen,
        CurlyClose,
        Quote,
        Decimal,
        Number,
        SquareOpen,
        SquareClose,
        Word,
        Escaped,
        Unicode
    }
}